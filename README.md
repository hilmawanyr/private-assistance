# Private Assistance

This project was created to fulfill the assignment of Python Programming Course.
First you have to duplicate `.env.example` and `data.json.example` then rename them without `.example`.
And the last please do `uv sync` to install all dependencies, and then `uv run main.py` to run the app.
